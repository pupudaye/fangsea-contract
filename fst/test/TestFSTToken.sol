pragma solidity ^0.4.24;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/FSTToken.sol";

contract TestFSTToken {

//  uint public initialBalance = 10 ether;
//  FSTToken private fSTToken;
//
//  function () public {
//    // This will NOT be executed when Ether is sent. \o/
//  }
//  function beforeAll() public {
//    fSTToken = FSTToken(DeployedAddresses.FSTToken());
//  }

//  function testInitialBalance() public {
//    Assert.equal(fSTToken.balanceOf(msg.sender), 0, "Owner should have 0 Token initially");
//  }

//  function testIsMinter()public {
//    Assert.equal(fSTToken.isMinter(msg.sender), true, "msg sender is minter?");
//  }

//  function testHasCap()public{
//    Assert.equal(fSTToken.cap(), 100000, "msg sender has cap 100000");
//  }
//  function testMintToken()public{
//    Assert.equal(fSTToken.mint(msg.sender,100000), true, "Token mint is true at Owner");
//  }
}
